import * as React from 'react'

export interface Props {
    characterType: "player" | "pet";
    characterName: string;
    message: string;
}

export function ChatMessage({characterType,
    characterName,
    message}: Props) {

    return (
        
    )
}